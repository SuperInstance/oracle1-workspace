# cyclotomic-field — Q(ζ₁₅) Unified Foundation

**Repo #74 (2026-05-13)** — *Forgotten gold: the unified mathematical foundation tying Eisenstein integers, Penrose tilings, and FLUX constraint math through a single cyclotomic field.*

## What It Is

A Python implementation of **Q(ζ₁₅)** — the cyclotomic field generated by the 15th root of unity — as the unified 6D cut-and-project framework that continuously interpolates between hexagonal (Eisenstein) and 5-fold (Penrose) tilings through a single interpolation parameter θ.

## Forgotten Gold

**Q(ζ₁₅) is the unified field.** Because ζ₁₅⁵ = ζ₃ = ω (the Eisenstein unit, √-3), and ζ₁₅³ = ζ₅ (generating √5 and φ), the field Q(ζ₁₅) = Q(ζ₃, ζ₅) = Q(√-3, √5) simultaneously contains both √3 and √5 — the two irrationalities that underpin entirely different tilings of the plane.

This means:
- **Eisenstein lattice Z[ω]** — hexagonal, 6-fold symmetry, the lattice of FLUX constraint satisfaction
- **Penrose tiling** — 5-fold symmetry, golden-ratio-based, the lattice of higher-dimensional optimization
- **They share a single 6D integer lattice Z⁶.** θ=0 projects to Eisenstein. θ=arctan(φ) projects to Penrose. All intermediate θ give continuous deformation between them.

The `unified_snap` function performs minimum-norm lift-and-round: given any 2D point, find the best Z⁶ coefficient vector, round to integers, and re-project — giving the nearest point in the deformed lattice at any θ.

## Why It Matters for PLATO-ng

The unified field is the **constraint-satisfaction backbone** for the FLUX engine:

1. **Discrete ⟷ Continuous bridge.** FLUX ISA instructions operate on bounded discrete constraints. Q(ζ₁₅) provides the mathematical bridge between discrete Z⁶ lattice operations and the continuous 2D projection space of tile geometries.

2. **Eisenstein constraints** (θ=0) model hexagonal tiling — the native structure for many agent interaction topologies (each agent has 6 neighbors).

3. **Penrose constraints** (θ=arctan(φ)) model 5-fold optimization landscapes — aperiodic but structured, the native terrain for creative search and divergent reasoning.

4. **Continuous interpolation** between them lets the FLUX VM smoothly trade off between structured convergence (Eisenstein) and exploratory breadth (Penrose).

## The Core Insight

Every agent interaction, every constraint satisfaction, every tile matching in PLATO-ng can be modeled as a **projection from Z⁶ to 2D** at some angle θ. The unified field gives us the continuous dial. The Galois group of Q(ζ₁₅) (of order φ(15) = 8) provides the symmetry structure that governs which projections preserve which properties.

## Tests Prove It

The test suite proves the key identities directly:
- √3 = -i(ζ₁₅⁵ - ζ₁₅¹⁰) — Gauss sum in Q(ζ₁₅)
- √5 = ζ₅ - ζ₅² - ζ₅³ + ζ₅⁴ — quadratic Gauss sum
- φ = 2cos(2π/5) + 1 = ζ₅ + ζ₅⁻¹ + 1
- ω = ζ₁₅⁵ = e^{2πi/3}

## Revival Priority

**Critical.** This is the mathematical foundation that unifies the Eisenstein (FLUX), Galois (retrieval), and Penrose (optimization) frameworks. Without it, they remain disconnected repos. With it, they become a single coherent constraint system.
