# Cocapn Fleet — Monorepo Architecture & Roadmap

> *Auto-generated by Oracle1. Last sweep: 2026-05-03 (evening).*
>
> Audits: `flux-research/audit/plato-stack-audit.md`, `flux-research/audit/agent-stack-audit.md`, `flux-research/audit/flux-stack-audit.md`

---

## The Big Picture

The Cocapn Fleet has ~1397 repos. This is not chaos — it's a **fleet ecosystem**. But it needs better organization to be navigable.

**The organizing principle:** The fleet is organized around **7 stacks**, not alphabetical repo lists. Each stack is a coherent system with a purpose.

---

## The 9 Stacks

### Stack 1: Core Fleet Infrastructure ⭐
*What keeps the fleet running.*

| Repo | Purpose | Status |
|------|---------|--------|
| `plato-server` | Production PLATO server — SQLite, Docker, fleet sync | ✅ Running on :8847 |
| `plato-room-phi` | Phi computation layer — measures room integration quality | ✅ On top of server |
| `oracle1-workspace` | Oracle1's working context + services | ✅ Running |
| `oracle1-index` | Searchable index of all repos (690+) | ✅ Running |
| `holodeck-rust` | GPU-accelerated simulation environment | ⚠️ Recent push |
| `holodeck-core` | Core MUD engine (no_std crate) | ⚠️ Recent push |
| `cocapn-core` | Core library | ⚠️ Recent push |
| `cocapn` | Main package | ⚠️ Recent push |
| `cocapn-glue-core` | Keeper↔Fleet binary wire protocol (msgpack) | ✅ **NOW ON SuperInstance** |

---

### Stack 2: PLATO 🧠
*Shared knowledge lattice. The fleet brain.*

### Core Server
| Repo | Purpose | Status |
|------|---------|--------|
| `plato-server` | Production PLATO server — SQLite, Docker, fleet sync | ✅ Running on :8847 |
| `plato-room-phi` | Integrated Information (Phi) computation | ✅ On top of server |

### PLATO Variants (4 different approaches)
| Repo | Language | Focus |
|------|---------|-------|
| `plato-kernel` | Rust | Central state machine, DCS flywheel, deadband governance |
| `plato-dcs` | Rust | DCS flywheel, dynamic locks consensus |
| `plato-mythos` | Python | Recurrent-Depth Transformer, rooms as MoE experts |
| `plato-edge` | Python | ARM64 minimal (<100KB, zero deps) |

### Tools & SDKs
| Repo | Language | What |
|------|---------|-----|
| `plato-sdk` | Python | Agent SDK for PLATO |
| `plato-sdk-unified` | Python | **Archived** — depends on non-existent PyPI packages |
| `plato-cli` | Rust | Single binary — search tiles, check deadband |
| `plato-tutor` | Python | Context jumping with WordAnchor extraction |
| `plato-mud-server` | Python | Text-based training ground (16 rooms) |
| `plato-client-php` | PHP | PHP 8.0+ PLATO client library |
| `superinstance-flux-runtime-ruby` | Ruby | Ruby FLUX VM with metaprogramming |
| `plato-attention-tracker` | Python | Attention tracking |
| `plato-surprise-detector` | Python | Surprise detection in tile streams |
| `mud-mcp` | TypeScript | MCP bridge for MUD |

**Key insight:** `plato-server` is the server. `plato-room-phi` is a scoring layer ON TOP. The 4 variants (kernel/dcs/mythos/edge) are different approaches — could become feature flags in one crate.

---

### Stack 9: Agent Vessels 🌊
*Per-domain agents with landing pages.*

Pattern: `domain-agent` (Python, CLI) + `domain-ai-pages` (GitHub Pages).

**Coverage: 12/12 domain agents have landing pages** ✅

| Domain | Agent | Landing | PLATO Schema |
|--------|-------|---------|-------------|
| activelog | ✅ | ✅ | ❌ undocumented |
| activeledger | ✅ | ✅ | ❌ undocumented |
| businesslog | ✅ | ✅ | ❌ undocumented |
| cocapn | ✅ | ✅ | Landing page |
| captaine | ✅ | ✅ | Pages + agent |
| dmlog | ✅ | ✅ | 🆕 recent |
| deckboss | ✅ | ✅ | ✅ explicit |
| fishinglog | ✅ | ✅ | ✅ explicit |
| luciddreamer | ✅ | ✅ | 🆕 recent |
| makerlog | ✅ | ✅ | 🆕 recent |
| personallog | ✅ | ✅ | 🆕 recent |
| playerlog | ✅ | ✅ | 🆕 recent |
| reallog | ✅ | ✅ | 🆕 recent |
| studylog | ✅ | ✅ | ✅ explicit |

**Biggest gaps:**
- Most agents claim PLATO but don't document tile schemas
- `superinstance-plato-sdk` v2.0.0 is canonical (pip install plato_sdk) — 14/14 domain agents use it
- `plato-sdk-unified`: **Archived** — depended on non-existent PyPI packages
- Test coverage inconsistent across agents

---

### Stack 4: FLUX Runtime 🏗️
*The compiler, runtime, and bytecode that runs on vessels.*

**Architecture:** FLUX.MD (structured markdown) → 6-plane compiler → 64-register bytecode VM → native/edge

| Repo | Language | Purpose |
|------|---------|---------|
| `flux` | Rust | High-performance runtime — 64-register VM, SSA IR |
| `flux-runtime` | Python | Reference impl — 2,037 tests, zero deps |
| `flux-runtime-c` | C | C port for edge deployment |
| `flux-compiler` | Python | 6-plane abstraction compiler |
| `flux-reasoner` | Python | Dual-interpreter gradient reasoning engine |
| `flux-discussion-flows` | Python | Three-tier adversarial debate system |
| `flux-os` | C | Microkernel OS — kernel IS the compiler |
| `flux-research` | Python | Research playground — ISA v3, DCS protocol, 60K+ words |
| `flux-vm-php` | PHP | Pure PHP FLUX VM — 100+ opcodes, assembler, CLI |
| `agentic-compiler` | Python | Markdown-to-runtime with swarm deliberation ⚠️ *may duplicate flux-compiler* |

**FM's Stack 4 additions (this session):**
| `guard2mask` | Python | GUARD constraint → FLUX bytecode compiler |
| `flux-ast` | Python | Abstract syntax tree for FLUX |
| `flux-bridge` | Python | FLUX-C ↔ FLUX-X bridge (byte-compat proven 7/7) |
| `guardc` | Python | Verified GUARD compiler (11/11 tests passing) |

**Redundancies:**
- `flux-compiler-agentic` — **ARCHIVED** (duplicate of flux-compiler)
- `flux-reasoner-engine` — **ARCHIVED** (duplicate of flux-reasoner)

**Key insight:** DCS protocol (Divide-Conquer-Synthesize) yields **5.88× specialist** and **21.87× generalist** improvement. Protocol design > model capability.

**Connection to PLATO:** PLATO stores knowledge → git-agent writes FLUX.MD → flux-compiler → flux-runtime → execute

**Biggest gaps:**
- FLUX ISA spec (247 opcodes) not documented in one place — ✅ RESOLVED: flux-isa-v3.md
- FLUX → PLATO bridge not documented — ✅ RESOLVED: Section 0 architecture doc added

---

### Stack 5: Greenhorn Dojo 🎓
*The agent growth system.*

| Repo | Purpose | Status |
|------|---------|--------|
| `greenhorn` | Core dojo model + .spark/ | ✅ Just updated |
| `greenhorn-runtime` | Portable deployment runtime + .spark/ | ✅ Just updated |
| `greenhorn-onboarding` | Zero-config entry point + .spark/ | ✅ Just updated |
| `bootstrap-spark` | **Canonical** `.spark/` template + validator | 🆕 **NEW — FM's canonical implementation** |

**Status:** Clean and well-organized. Bootstrap Spark is now the fleet standard.

---

### Stack 6: Constraint Theory ⚡
*FLUX-C safety and constraint-driven execution.*

| Repo | Purpose | Status |
|------|---------|--------|
| `constraint-theory-core` | Core constraint theory library | ✅ Published |
| `ct-demo` | Demo/benchmark | ✅ Published |
| `guard2mask` | GUARD → bytecode compiler | ✅ FM's work |
| `guardc` | Verified GUARD compiler | ✅ 11/11 tests |
| `BitmaskDomain` | Bitmask domain solver | ✅ FM's work |

---

### Stack 7: Equipment ⚙️

| Repo | Purpose | Status |
|------|---------|--------|
| `purplepincher` | Main repo | ✅ Updated today |
| `purplepincher-org-pages` | Live website (purplepincher.org) | ✅ Updated today |
| `purplepincher-shell-library` | Shell library | 🆕 Recent |
| `flux-research` | Research papers + whitepapers | ✅ Updated today |

**Status:** Public technical identity. Working well.

---

### Stack 8: Research & Exploration 🔬
*Experimental and forward-looking work.*

**Coverage: 12/12 domain agents have landing pages** ✅

| Repo | Language | Purpose |
|------|---------|---------|
| `Equipment-Consensus-Engine` | TypeScript | Consensus implementation |
| `Equipment-Swarm-Coordinator` | TypeScript | Swarm coordination |
| `Equipment-Escalation-Router` | TypeScript | Escalation routing |
| `Equipment-Consensus-Engine-PHP` | PHP | PHP port of consensus engine |
| `Equipment-Consensus-Engine-Ruby` | Ruby | Ruby port of consensus engine |
| `Equipment-Swarm-Coordinator-Ruby` | Ruby | Ruby port of swarm coordinator |
| `cudaclaw` | Rust | Rust CUDA |
| `DeepGEMM` | Deep GEMM kernels |
| `gpu-native-room-inference` | GPU inference |
| `marine-gpu-edge` | Marine GPU edge |
| `jepa-perception-lab` | JEPA perception |

---

This is Stack 9 — agent vessels — move to line 71 properly

| Repo | Purpose |
|------|---------|
| `abstraction-planes` | The Middle Way — 6-plane decomposition (Intent→Domain→IR→Bytecode→Native→Metal) |
| `bordercollie` | Herding 10,000 local CUDA agents with memories and skills |
| `AIR` | Asynchronous Infinite Radio — nightly synthesis, real-time learning, simulations |
| `barracks` | Agent accommodation system |
| `ai-character-sdk` | Unified AI Character SDK — escalation engine + hierarchical memory + learning |

---

## Live Status

| Metric | Value |
|--------|-------|
| Total repos | ~1397 |
| Updated this week | 150+ |
| PLATO stack repos | 20+ |
| FLUX stack repos | 10+ |
| Domain agent pairs | 12 |
| Landing page repos | 15+ |

---

## Roadmap

### ✅ P0 — RESOLVED: cocapn-glue-core on SuperInstance
- **`cocapn-glue-core`** — Keeper↔Fleet binary wire protocol. msgpack-based message framing for heartbeat, status, commands, and PLATO tile forwarding. ✅ NOW ON SUPERINSTANCE.

### Phase 1: Consolidate Core (This Week)
- [x] **Publish cocapn-glue-core to SuperInstance** ✅
- [x] **Archive `flux-compiler-agentic`** — duplicate of flux-compiler ✅
- [x] **Archive `flux-reasoner-engine`** — duplicate of flux-reasoner ✅
- [ ] **Investigate agentic-compiler** — is it redundant with flux-compiler?
- [x] **Consolidate domain agents onto shared scaffold** ✅ — 14/14 agents using fleet-agent base class
- [ ] **Update cocapn.ai landing page** with new Bootstrap Stack

### Phase 2: PLATO Cleanup (This Week)
- [ ] **Consolidate 4 PLATO variants** — feature flags in one crate, or pick "production" variant
- [ ] **Consolidate holodeck-rust + holodeck-core** — GPU sim + core engine one crate
- [x] **Pick one SDK** ✅ — superinstance-plato-sdk v2.0.0 is canonical
- [ ] **Document which PLATO variant is "production"**

### Phase 3: FLUX Consolidation (Next Week)
- [ ] **Write FLUX ISA spec** — 247 opcodes in one document
- [ ] **Document FLUX→PLATO bridge**
- [ ] **Connect git-agent to FLUX compiler pipeline**
- [ ] **Resolve agentic-compiler vs flux-compiler overlap**

### Phase 4: Landing Page Audit (Next Week)
- [ ] Verify all 15+ `-ai-pages` repos have working pages
- [ ] Add `.spark/` to each domain agent repo
- [ ] Connect landing pages to Bootstrap Spark

### Phase 5: Equipment Integration (Ongoing)
- [ ] Document what each Equipment repo does
- [ ] Investigate and integrate new repos (barracks, ai-character-sdk, AIR, bordercollie, abstraction-planes)

---

## Recommended Monorepo Structure

```
cocapn-fleet/
├── core/                    # Core fleet infrastructure
│   ├── plato-server/      # PLATO server + room-phi
│   ├── oracle1-workspace/   # Oracle1 working context
│   ├── holodeck/          # MUD engine (rust + core)
│   ├── cocapn-core/       # Core library
│   └── cocapn-glue-core/ # Keeper↔Fleet wire protocol
│
├── plato-extensions/       # All PLATO variants + tools
│   ├── plato-kernel/     # Rust kernel variant
│   ├── plato-dcs/        # DCS variant
│   ├── plato-mythos/     # Transformer variant
│   ├── plato-edge/       # Edge variant
│   ├── plato-sdk/        # Agent SDK
│   ├── plato-cli/         # CLI binary
│   ├── plato-tutor/       # Tutoring
│   └── mud-mcp/           # MCP bridge
│
├── flux/                   # FLUX ISA, compiler, runtime
│   ├── flux/             # Rust high-perf runtime
│   ├── flux-runtime/    # Python reference impl
│   ├── flux-runtime-c/ # C edge port
│   ├── flux-compiler/   # 6-plane compiler
│   ├── flux-os/         # C microkernel
│   ├── flux-research/   # Research + ISA spec
│   └── flux-reasoner/   # Reasoning engine
│
├── agents/                 # Agent scaffold + domain agents
│   ├── scaffold/          # shared-agent-scaffold
│   ├── domain/           # activelog, deckboss, etc.
│   ├── barracks/          # Agent accommodation
│   └── standalone/       # keeper-agent, trust-agent, etc.
│
├── dojo/                  # Greenhorn dojo system
│   ├── greenhorn/        # Core dojo
│   ├── greenhorn-runtime/ # Deployment runtime
│   ├── greenhorn-onboarding/ # Entry point
│   └── bootstrap-spark/  # Canonical .spark/ template
│
├── purplepincher/         # Agent shell technology
│   ├── purplepincher/    # Main repo
│   └── purplepincher-org-pages/ # purplepincher.org
│
├── equipment/             # Specialized hardware/systems
│   ├── consensus/        # Equipment-*
│   └── gpu/             # DeepGEMM, cudaclaw, etc.
│
├── research/              # Experimental work
│   ├── abstraction-planes/ # 6-plane decomposition framework
│   ├── bordercollie/     # CUDA agent herding
│   ├── AIR/              # Nightly synthesis
│   ├── ai-character-sdk/  # Unified character SDK
│   └── flux-research/     # Papers and research
│
├── landing/               # Landing pages per domain
│
└── meta/                  # Index, docs, organization
    └── oracle1-index/    # Searchable repo index
```

---

*Last updated by Oracle1: 2026-05-03 (evening)*
*The ocean counts. The Spark lights the fire.*

## Updates — 2026-05-03 Evening

### Completed
- ✅ **flux-compiler-agentic ARCHIVED** — duplicate of flux-compiler, archived via gh CLI
- ✅ **flux-reasoner-engine ARCHIVED** — duplicate of flux-reasoner, archived via gh CLI
- ✅ **cocapn.ai landing page REWRITTEN** — Bootstrap Stack diagram, fleet roster (4 vessels), papers showcase, dojo model, links. Pushed live.

### Note
- Cannot delete repos (no `delete_repo` scope on current token). Archived instead (same effect, repo is hidden).
- `flux-compiler` pyproject.toml was already correct (subagent report had wrong info).

### Phase 4 Progress — 2026-05-03 Evening
- ✅ **fishinglog-agent** — .spark/ added
- ✅ **deckboss-agent** — .spark/ added
- ✅ **studylog-agent** — .spark/ added
- ✅ **businesslog-agent** — .spark/ added
- ✅ **activelog-agent** — .spark/ added
- ✅ **makerlog-agent** — .spark/ added
- ✅ **personallog-agent** — .spark/ added
- ✅ **dmlog-agent** — .spark/ added
- ✅ **reallog-agent** — .spark/ added
- ✅ **playerlog-agent** — .spark/ added
- ✅ **luciddreamer-agent** — .spark/ added
- ⬜ **activeledger-agent** — still needs .spark/
- ⬜ **capitaine-agent** — still needs .spark/
- ⬜ **lucineer** — still needs .spark/
- ⬜ **bordercollie** — skeleton, needs real content first
- ⬜ **agentic-compiler** — skeleton, needs real content first
- ⬜ **AIR** — skeleton, needs real content first

### Phase 4 COMPLETE — 2026-05-03 Evening
All 13 SuperInstance domain agents now have .spark/ directories:
- ✅ fishinglog-agent, deckboss-agent, studylog-agent
- ✅ businesslog-agent, activelog-agent, makerlog-agent, personallog-agent
- ✅ dmlog-agent, reallog-agent, playerlog-agent, luciddreamer-agent
- ✅ activeledger-agent, capitaine-agent

Note: lucineer/lucineer (Magnus's account, family privacy) — cannot push there, but the SuperInstance forks have .spark/ already.

### FM's New Pushes — 2026-05-03 Evening
FM is on fire. Just pushed 7 new repos:

1. **bootstrap-spark** — `.spark/` template + validator. **Canonical implementation.** This is the fleet standard for self-describing agent knowledge. 6 markdown files: NAME, CONTEXT, FLAGS, PROTOCOL, TODO, HISTORY.
2. **abstraction-planes** — The Middle Way. 6-plane stack (Intent → Domain → IR → Bytecode → Native → Metal). Finds optimal plane for decomposition. Plane 4 (Domain Language/FLUX-ese) is the sweet spot.
3. **bordercollie** — Herding 10,000 local CUDA agents with memories and skills. (No .spark/ — CUDA infrastructure, not a domain agent)
4. **agentic-compiler** — Markdown-to-runtime with swarm deliberation, A/B experimentation, git-native evolution. **⚠️ Possible redundancy with flux-compiler** — needs investigation.
5. **barracks** — Agent accommodation system. Where agents live.
6. **ai-character-sdk** — Unified AI Character SDK combining escalation engine, hierarchical memory, and learning into simple API.
7. **AIR** — Asynchronous Infinite Radio. Nightly synthesis for morning briefing, real-time interactive learning, simulations, ideation. Build a wiki as you chat.

### cocapn-glue-core — ✅ NOW ON SUPERINSTANCE
- **Status:** P0 task RESOLVED
- **Description:** Keeper↔Fleet binary wire protocol. msgpack-based message framing for heartbeat, status, commands, and PLATO tile forwarding. The nervous system of the Cocapn Fleet.

### Papers Published — 2026-05-03 Evening
- ✅ **Semantic Compiler** (2026-05-03) — intent → FLUX-ese → bytecode. 6-plane framework.
✅ **Semantic Compiler v5** (2026-05-05) — NL→GUARD→FLUX-C→Z3 proof, 195 lines
- ✅ **Compiled Agency** (2026-05-03) — agents as compiled artifacts, not running processes.
- ✅ **Bootstrap Spark + Bomb + Counting Before Flowing** — earlier today
- ✅ **Tide Pool Security** — earlier

### Redundancy Notes
| Archived Repo | Was Duplicate Of | Status |
|---------------|------------------|--------|
| `flux-compiler-agentic` | `flux-compiler` | ✅ Archived |
| `flux-reasoner-engine` | `flux-reasoner` | ✅ Archived |
| `agentic-compiler` | `flux-compiler` | ⚠️ **Needs investigation** — may be different enough to justify |

### New Stack Added — Stack 8: Research & Exploration 🔬
Added `abstraction-planes`, `bordercollie`, `AIR`, `barracks`, `ai-character-sdk` to new Stack 8 for experimental/forward-looking work.

---

## FM Stack Updates (2026-05-03 late)

### Stack 4 — FLUX Compiler & Runtime (FM additions)
| Repo | Purpose | Status |
|------|---------|--------|
| `guard2mask` | GUARD → FLUX bytecode compiler | ✅ FM built |
| `flux-ast` | Abstract syntax tree | ✅ FM built |
| `flux-bridge` | FLUX-C ↔ FLUX-X bridge | ✅ 7/7 tests passing |
| `guardc` | Verified GUARD compiler | ✅ 11/11 tests |
| `flux-isa` (5 variants) | ISA docs across variants | ✅ documented |

### Stack 6 — Constraint Theory (FM additions)
| Repo | Purpose | Status |
|------|---------|--------|
| `constraint-theory-core` | Core library | ✅ FM built |
| `ct-demo` | Benchmark suite | ✅ FM built |
| `BitmaskDomain` | Constraint solver | ✅ FM built |

### Key FM Research Finding
> **NO GPU has ASIL D or DAL A certification.** This is our opening — FLUX-C provides formally-verifiable constraint enforcement as a software layer below the GPU.

### Stack 8 — Research (expanded)
| Repo | Purpose | Status |
|------|---------|--------|
| ~200 PLATO tiles | FM's deep research submissions | ✅ in PLATO |

---

*Last FM update: 2026-05-03 23:48 UTC*
